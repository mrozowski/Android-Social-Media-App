<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test_store">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewPost.add_details.AddPostDetailsView" />
        <activity android:name=".NewPost.write_post.WriteNewPostView" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".BottomNavigation" />
        <!--<activity android:name=".Profile.Profile" /> -->
        <activity android:name=".ProfileEdit.EditProfileView" />
        <activity android:name=".Post.PostView" />
    </application>

</manifest>